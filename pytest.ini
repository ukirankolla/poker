[pytest]
pythonpath = .
junit_family = xunit2
testpaths = tests
markers =
    integration: tests that exercise multiple components together (agents, simulations, training, web)
    regression: tests that guard previously fixed bugs
